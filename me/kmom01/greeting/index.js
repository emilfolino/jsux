/**
 * Function that returns a greeting to a given name.
 * Implement the functionality using the template literal.
 * @param  {[string]} name Name of the person we want to greet.
 * @return {[string]}     A greeting containing 'Hello [name]'.
 */
function greeting (name) {
    return `Hello ${name}`;
}

module.exports = greeting;
